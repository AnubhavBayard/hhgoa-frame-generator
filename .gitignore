node_modules
.next
.env*.local
.vercel

# 14M of source photos — tools/cut-*.py turned these into public/*.png
user_inputs

# vercel link wrote this; narrowed so .env.example stays tracked
.env
.env*
