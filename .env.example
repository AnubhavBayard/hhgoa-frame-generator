# Vercel Blob store, used only for the "Share to X" link preview.
# Create a Blob store in the Vercel dashboard, then `vercel env pull .env.local`.
# Without it the app still works: Share to X downloads the PNG and opens a
# pre-filled post for the user to attach it to.
BLOB_READ_WRITE_TOKEN=
